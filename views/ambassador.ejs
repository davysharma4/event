<!DOCTYPE html>
<html>
    <head>
        <%- include('partials/head') %>
        <link rel='stylesheet' href='/static/stylesheets/home.css' />
    </head>
    <body>
        <%- include('partials/navbar')%>
        <div class="main">
            <%- include('partials/flash') %>
            <h2>Campus Ambassadors</h2>
            <%if(!ca){%>
                <div>
                    You are not a Campus Ambassador<br>
                    Be a Campus Ambassador. Promote Paradis by inviting people via email and earn exciting prizes.<br>
                    Post a request to be a Campus Ambassador.<br><br>
                    <form action="/campusAmbassadors/requests" method="POST">
                        <button type="submit" class="btn btn-lg btn-danger">Request</button>
                    </form>
                </div>
                <%}%>
            <%if(ca){%>
                <div>
                    You are a Campus Ambassador.<br>
                    Help us by inviting people to Paradis website via email.
                    <form action="/campusAmbassadors/invite" method="POST">
                        <div class="form-group row">
                            <div class="col-md-4 col-md-offset-4 center_div">
                                <label for="email">Just enter the email  of the person to be invited here:</label>
                                <input type="email" class="form-control" placeholder="Enter email" name="email" required>
                            </div>
        
                        </div>
                        <button type="submit" class="btn btn-lg btn-danger">Invite</button>
                    </form>
                </div>
                <%}%>
        </div>
        <%- include('partials/footer') %>
    </body>
</html>